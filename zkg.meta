[package]
script_dir = scripts
test_command = cd testing && btest -c btest.cfg
summary = Extends the http.log by adding all headrs that apeers in http_all_log event.
description = Extends the http.log by adding all headrs that appears 
	in some pcaps that were tested on.
depends = 
	zeek >=4.0.0

[template]
source = https://github.com/naorbarlev/http_all_headrs
version = v3.0.1
zkg_version = 2.14.0

[template_vars]
name = Http_all_headrs

